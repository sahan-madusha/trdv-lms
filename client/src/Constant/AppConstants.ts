import config from "./config";

//common config
export const WEBNAME = config.webName;
export const ADDRESS = config.address;
export const EMAIL = config.email;
export const MOBILE = config.mobile;
export const WHATSAPP = config.whatsapp;
export const NO_IMAGE = config.noImage;
export const DEVS_SITE = config.devsSite;

//env config
export const WEB_URL = config.webLink;
export const SERVER = config.server;
export const SERVER_API = config.serverapi;
export const IMAGE_URL = config.imagepath;

//web route
export const LOGIN = config.login;
export const LOGIN_SUCCESS = config.loginSuccess;
export const DASHBOARD = config.dashboard;
export const POS = config.pos;
export const USER_MANAGE = config.usermanage;
export const PRODUCT_MANAGE = config.productmanage;
export const PRODUCT_PURCHASE = config.productpurchase;
export const OORDER_MANAGE = config.ordermanage;

//commons
export const APPLOGO = config.appLogo;
export const SIGNIN_BG = config.signInBg;
export const PAGE_SIZE = 10;
export const DEBOUNCED_SEARCH_QUERY = 500;
